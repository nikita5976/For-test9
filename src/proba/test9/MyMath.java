package proba.test9;

import static java.lang.Math.*;

public class MyMath {
    public static void main(String[] args) {
        double radians = Math.toRadians(90);
        double resMath = sin (radians); //радианы
        System.out.println(resMath);
    }
}
